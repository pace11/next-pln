/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ["pln.pace-unv.cloud", "www.infouip3bs.com", "127.0.0.1"],
  },
};

export default nextConfig;
